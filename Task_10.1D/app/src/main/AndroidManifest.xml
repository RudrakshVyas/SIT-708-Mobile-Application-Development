<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.personalizedlearningapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Personalized Learning App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalizedLearningApp">

        <!-- Launcher -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User Flow Screens -->
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.InterestsActivity" />
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.RecommendationActivity" />
        <activity android:name=".activities.AssessmentActivity" />
        <activity android:name=".activities.ResultActivity" />

        <!-- Enhanced Features for Task 10.1D -->
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.HistoryActivity" />
        <activity android:name=".activities.HistoryDetailActivity" />
        <activity android:name=".activities.UpgradeActivity" />

    </application>

</manifest>
